import styled from 'styled-components';

export const Burguer = styled.div`
  margin-top: 0.5rem;
  position: relative;
  cursor: pointer;

  span {
    display: block;
    width: 50px;
    height: 6px;
    background-color: black;
    margin-bottom: 10px;
  }
`;
